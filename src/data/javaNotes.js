/**
 * Java interview notes — structured, interview-ready content.
 * Rendered by src/pages/JavaNotes.jsx via a small block renderer.
 *
 * Block types:
 *  { type: 'p', text }                     paragraph
 *  { type: 'answer', text }                "say this in the interview" callout
 *  { type: 'h', text }                     sub-heading
 *  { type: 'ul', items: [] }               bullet list
 *  { type: 'code', code }                  code block
 *  { type: 'table', head: [], rows: [[]] } comparison table
 *  { type: 'qa', items: [{ q, a }] }       tricky questions
 *  { type: 'tip', text }                   interview tip
 */

export const javaNotes = [
  {
    id: 'jvm',
    title: 'JVM, JRE & JDK',
    icon: 'bolt',
    blocks: [
      { type: 'p', text: 'One of the most common opening questions. Remember the nesting: JDK = JRE + dev tools, JRE = JVM + libraries, JVM = runs bytecode.' },
      {
        type: 'table',
        head: ['Feature', 'JVM', 'JRE', 'JDK'],
        rows: [
          ['Full form', 'Java Virtual Machine', 'Java Runtime Environment', 'Java Development Kit'],
          ['Purpose', 'Executes bytecode', 'Runs Java apps', 'Develops Java apps'],
          ['Contains', 'Class loader, memory, execution engine', 'JVM + core libraries', 'JRE + compiler & tools'],
          ['Run a program', 'Yes', 'Yes', 'Yes'],
          ['Develop a program', 'No', 'No', 'Yes'],
        ],
      },
      { type: 'answer', text: 'JVM executes the Java bytecode. JRE provides the environment to run Java applications and includes the JVM and libraries. JDK is used to develop Java programs and includes the JRE along with tools like the compiler.' },
      { type: 'h', text: 'Is the JVM platform independent?' },
      { type: 'ul', items: ['Java is platform independent — because source code compiles to bytecode.', 'The JVM itself is platform dependent — each OS (Windows, Linux, macOS) has its own JVM implementation.', 'Bytecode can run on any JVM, but the JVM depends on the OS.'] },
      { type: 'answer', text: 'Java is platform independent because it uses bytecode, but the JVM itself is platform dependent because different operating systems have different JVM implementations.' },
      { type: 'h', text: 'What happens inside the JVM during execution?' },
      { type: 'ul', items: ['Compilation: .java → javac → .class (bytecode).', 'Class Loader: Bootstrap → Extension → Application loaders load the class.', 'Bytecode verification: checks for illegal code / memory violations.', 'Runtime data areas: Method Area, Heap, Stack, PC Register, Native Method Stack.', 'Execution engine: Interpreter + JIT compiler, with the Garbage Collector managing memory.'] },
      {
        type: 'qa',
        items: [
          { q: 'Can we run a Java program without the JDK?', a: 'Yes, with the JRE — but you cannot compile.' },
          { q: 'Can we run a Java program without the JVM?', a: 'No, the JVM is mandatory.' },
          { q: 'What is the JIT compiler?', a: 'It converts frequently used bytecode into native machine code for speed.' },
          { q: 'Which memory stores objects?', a: 'Heap memory.' },
        ],
      },
    ],
  },
  {
    id: 'stack-heap',
    title: 'Stack vs Heap',
    icon: 'layers',
    blocks: [
      { type: 'p', text: 'Stack stores method calls & local variables; Heap stores objects & instance variables.' },
      {
        type: 'table',
        head: ['Feature', 'Stack', 'Heap'],
        rows: [
          ['Used for', 'Method execution', 'Object storage'],
          ['Stores', 'Local variables, method calls', 'Objects, instance variables'],
          ['Allocation', 'Static (LIFO)', 'Dynamic'],
          ['Speed', 'Fast', 'Slower'],
          ['Management', 'Automatic', 'Garbage Collector'],
          ['Scope', 'Thread-specific', 'Shared among threads'],
          ['Error', 'StackOverflowError', 'OutOfMemoryError'],
        ],
      },
      { type: 'answer', text: 'Stack memory is used for method execution and stores local variables, while heap memory stores objects and instance variables. Stack is thread-safe and faster; heap is shared and managed by the garbage collector.' },
      { type: 'code', code: 'class Test {\n    int x = 10;              // Heap (inside object)\n\n    public static void main(String[] args) {\n        int a = 5;           // Stack\n        Test t = new Test(); // reference on Stack, object on Heap\n    }\n}' },
      {
        type: 'qa',
        items: [
          { q: 'Why is Stack faster than Heap?', a: 'It follows LIFO and needs no garbage collection.' },
          { q: 'Is Stack thread-safe?', a: 'Yes, each thread has its own stack.' },
          { q: 'Where is a String stored?', a: 'The String object is on the Heap; the reference is on the Stack.' },
          { q: 'Which memory causes StackOverflowError?', a: 'Stack memory (e.g. deep/infinite recursion).' },
        ],
      },
    ],
  },
  {
    id: 'typecasting',
    title: 'Typecasting',
    icon: 'route',
    blocks: [
      { type: 'answer', text: 'Typecasting in Java is the process of converting one data type into another, either automatically or manually.' },
      { type: 'h', text: 'Implicit (Widening)' },
      { type: 'ul', items: ['Smaller → larger type, done automatically by Java.', 'No data loss.', 'Order: byte → short → int → long → float → double'] },
      { type: 'code', code: 'int a = 10;\ndouble b = a;   // implicit\nSystem.out.println(b); // 10.0' },
      { type: 'h', text: 'Explicit (Narrowing)' },
      { type: 'ul', items: ['Larger → smaller type, done manually by the programmer.', 'Data loss is possible.'] },
      { type: 'code', code: 'double x = 10.5;\nint y = (int) x; // explicit\nSystem.out.println(y); // 10' },
      { type: 'h', text: 'Object typecasting' },
      { type: 'code', code: 'class A {}\nclass B extends A {}\n\nA obj = new B();   // Upcasting (implicit)\nB obj2 = (B) obj;  // Downcasting (explicit)' },
      { type: 'ul', items: ['Upcasting → implicit.', 'Downcasting → explicit (may cause ClassCastException).'] },
      {
        type: 'qa',
        items: [
          { q: 'Is boolean typecasting allowed?', a: 'No, boolean cannot be typecasted.' },
          { q: 'Does explicit casting always lose data?', a: 'No, but data loss is possible.' },
          { q: 'What happens if we downcast wrongly?', a: 'A runtime ClassCastException.' },
        ],
      },
    ],
  },
  {
    id: 'strings',
    title: 'Strings & String Constant Pool',
    icon: 'code',
    blocks: [
      { type: 'answer', text: 'String is a class in Java used to represent a sequence of characters, and it is immutable.' },
      { type: 'h', text: 'Why String is immutable' },
      { type: 'ul', items: ['Security (passwords, URLs).', 'Thread safety.', 'String Constant Pool optimisation.', 'Caching of hashcode.'] },
      { type: 'code', code: 'String s = "Java";\ns.concat(" World");\nSystem.out.println(s); // Java  (unchanged)' },
      { type: 'h', text: 'String Constant Pool (SCP)' },
      { type: 'ul', items: ['Special memory area inside the Heap.', 'Stores unique string literals to save memory.', 'Literals point to the same object; new String() creates a separate heap object.'] },
      { type: 'h', text: '== vs .equals()' },
      { type: 'p', text: '== compares references (memory address); .equals() compares content (value).' },
      { type: 'code', code: 'String s1 = "Java";\nString s2 = "Java";\nSystem.out.println(s1 == s2);      // true  (same SCP object)\nSystem.out.println(s1.equals(s2)); // true\n\nString s3 = new String("Java");\nString s4 = new String("Java");\nSystem.out.println(s3 == s4);      // false (different heap objects)\nSystem.out.println(s3.equals(s4)); // true' },
      { type: 'answer', text: 'The == operator compares references, while equals() compares the actual content of the String.' },
      { type: 'h', text: 'String vs StringBuffer vs StringBuilder' },
      {
        type: 'table',
        head: ['Feature', 'String', 'StringBuffer', 'StringBuilder'],
        rows: [
          ['Mutability', 'Immutable', 'Mutable', 'Mutable'],
          ['Thread-safe', 'Yes', 'Yes', 'No'],
          ['Performance', 'Slow', 'Medium', 'Fast'],
          ['Use case', 'Fixed data', 'Multi-thread', 'Single-thread'],
        ],
      },
      {
        type: 'qa',
        items: [
          { q: 'How many objects: String s = new String("Java");', a: 'Two — "Java" in the SCP and a new object in the Heap.' },
          { q: 'What does intern() do?', a: 'Returns the SCP reference for the string; so s1 == s1.intern() is false when s1 is a heap object.' },
          { q: 'Why is String preferred for passwords?', a: 'Immutability prevents modification (though char[] is even safer in practice).' },
          { q: 'Can String be inherited?', a: 'No, the String class is final.' },
        ],
      },
    ],
  },
  {
    id: 'oops',
    title: 'OOP: Overloading & Encapsulation',
    icon: 'grid',
    blocks: [
      { type: 'h', text: 'Method Overloading' },
      { type: 'answer', text: 'Method overloading allows multiple methods with the same name but different parameter lists, and is an example of compile-time polymorphism.' },
      { type: 'ul', items: ['Decided by the method signature: number, type, and order of parameters.', 'Return type alone is NOT enough to overload.', 'Java prefers an exact match over type promotion.'] },
      { type: 'code', code: 'int add(int a, int b) { return a + b; }\ndouble add(double a, double b) { return a + b; }\nint add(int a, int b, int c) { return a + b + c; }' },
      {
        type: 'qa',
        items: [
          { q: 'Can we overload the main method?', a: 'Yes.' },
          { q: 'Can we overload by changing only the return type?', a: 'No.' },
          { q: 'Can static methods be overloaded?', a: 'Yes.' },
        ],
      },
      { type: 'h', text: 'Encapsulation (real-world)' },
      { type: 'answer', text: 'Encapsulation is an OOP concept where data members are kept private and accessed only through public methods to protect data from unauthorized access.' },
      { type: 'p', text: 'Bank account analogy: you cannot touch the balance directly — you go through the ATM / bank services (public methods that validate).' },
      { type: 'code', code: 'class BankAccount {\n    private double balance;              // data hiding\n\n    public void deposit(double amount) {\n        if (amount > 0) balance += amount; // validation\n    }\n    public double getBalance() { return balance; }\n}' },
      { type: 'ul', items: ['Achieved by: private variables + public getters/setters.', 'Benefits: data security, controlled access, maintainability, flexibility.'] },
      { type: 'tip', text: 'Encapsulation = how data is protected; Abstraction = what functionality is shown.' },
    ],
  },
  {
    id: 'constructors',
    title: 'Constructors',
    icon: 'bolt',
    blocks: [
      { type: 'answer', text: 'A constructor is a special method in Java that is automatically called when an object is created and is used to initialize the object.' },
      { type: 'ul', items: ['Name = class name.', 'No return type (not even void).', 'Called automatically via new.', 'Cannot be inherited or overridden.'] },
      { type: 'h', text: 'Types' },
      { type: 'ul', items: ['Default (provided by Java if none written).', 'No-arg constructor.', 'Parameterized constructor.', 'Constructor overloading (same rules as method overloading).'] },
      { type: 'code', code: 'class Demo {\n    int x;\n    Demo() { this(10); }       // calls parameterized constructor\n    Demo(int x) { this.x = x; } // this differentiates instance variable\n}' },
      { type: 'h', text: 'Execution flow' },
      { type: 'ul', items: ['Memory allocation → instance variable initialization → constructor body executes.'] },
      {
        type: 'qa',
        items: [
          { q: 'Can a constructor be private?', a: 'Yes — used in the Singleton pattern.' },
          { q: 'Can a constructor be static or final?', a: 'No.' },
          { q: 'Are constructors inherited?', a: 'No.' },
          { q: 'Why does a constructor have no return type?', a: 'Because it initializes the object rather than returning a value.' },
        ],
      },
    ],
  },
  {
    id: 'inheritance',
    title: 'Inheritance',
    icon: 'route',
    blocks: [
      { type: 'answer', text: 'Inheritance is an OOP concept where a child class inherits the properties and methods of a parent class using the extends keyword.' },
      { type: 'h', text: 'Types' },
      { type: 'ul', items: ['Single: A → B.', 'Multilevel: A → B → C.', 'Hierarchical: one parent, many children.', 'Multiple inheritance with classes is NOT supported (allowed via interfaces).'] },
      { type: 'h', text: 'Why no multiple inheritance with classes?' },
      { type: 'p', text: 'To avoid the Diamond Problem — ambiguity when two parents define the same method.' },
      { type: 'h', text: 'extends vs implements' },
      {
        type: 'table',
        head: ['extends', 'implements'],
        rows: [
          ['Used with classes', 'Used with interfaces'],
          ['Single inheritance', 'Multiple inheritance'],
          ['Methods may have a body', 'Methods are abstract by default'],
        ],
      },
      { type: 'h', text: 'super keyword' },
      { type: 'ul', items: ['Access parent variables.', 'Call parent methods.', 'Call the parent constructor: super();'] },
      {
        type: 'qa',
        items: [
          { q: 'Can we inherit private members?', a: 'No, but they can be accessed via public methods.' },
          { q: 'Can a final method be overridden?', a: 'No.' },
          { q: 'What is an IS-A relationship?', a: 'Dog extends Animal → Dog IS-A Animal.' },
        ],
      },
    ],
  },
  {
    id: 'polymorphism',
    title: 'Polymorphism (deep)',
    icon: 'sparkles',
    blocks: [
      { type: 'answer', text: 'Polymorphism allows the same method name to perform different behaviors based on the object type.' },
      {
        type: 'table',
        head: ['Feature', 'Compile-time', 'Runtime'],
        rows: [
          ['Achieved by', 'Method overloading', 'Method overriding'],
          ['Binding', 'Early', 'Late (dynamic dispatch)'],
          ['Decision time', 'Compile', 'Runtime'],
        ],
      },
      { type: 'h', text: 'Key rule' },
      { type: 'p', text: 'Method call depends on the object; variable access depends on the reference.' },
      { type: 'code', code: 'class Parent { void show() { System.out.println("Parent"); } }\nclass Child extends Parent { void show() { System.out.println("Child"); } }\n\nParent p = new Child();\np.show(); // Child  (object type wins for methods)' },
      { type: 'h', text: 'Memory & dispatch' },
      { type: 'ul', items: ['Heap: the new Child() object (holds both Parent & Child methods).', 'Stack: the reference variable p.', 'Method Area: class definitions.', 'JVM resolves overridden calls via Dynamic Method Dispatch using a Virtual Method Table (V-Table).'] },
      {
        type: 'qa',
        items: [
          { q: 'Do variables participate in runtime polymorphism?', a: 'No — fields are resolved at compile time by reference type (p.x prints the Parent field).' },
          { q: 'Can static methods be overridden?', a: 'No — this is method hiding, not overriding.' },
          { q: 'Can private methods be overridden?', a: 'No, they are not visible to the child.' },
          { q: 'Overriding & exceptions?', a: 'The child method can only throw the same or a subclass (narrower) checked exception.' },
        ],
      },
      { type: 'tip', text: 'To sound senior: mention reference vs object, Heap & Stack, Dynamic Method Dispatch, and the V-Table.' },
    ],
  },
  {
    id: 'abstract',
    title: 'Abstract Class',
    icon: 'grid',
    blocks: [
      { type: 'answer', text: 'An abstract class is declared using the abstract keyword, can contain abstract and concrete methods, and is used to achieve abstraction.' },
      { type: 'code', code: 'abstract class Vehicle {\n    abstract void start();            // abstract\n    void fuel() { System.out.println("Needs fuel"); } // concrete\n}\nclass Car extends Vehicle { void start() { System.out.println("Key"); } }' },
      { type: 'ul', items: ['Cannot be instantiated.', 'Can have constructors, instance variables, static & main methods.', 'Can extend a class and implement interfaces.', 'Cannot be final; an abstract method has no body.', 'If a child does not implement all abstract methods, the child must also be abstract.'] },
      {
        type: 'qa',
        items: [
          { q: 'Can an abstract class have a constructor?', a: 'Yes — it runs when the child object is created.' },
          { q: 'Can an abstract method be private?', a: 'No.' },
          { q: 'Can an abstract class have a main method?', a: 'Yes.' },
        ],
      },
      { type: 'tip', text: 'Abstract class over interface when you need shared state (instance variables) and constructors — interfaces cannot have those.' },
    ],
  },
  {
    id: 'interface',
    title: 'Interfaces, Functional Interfaces & Java 8',
    icon: 'grid',
    blocks: [
      { type: 'answer', text: 'An interface is a collection of abstract methods and constants used to achieve abstraction and multiple inheritance.' },
      { type: 'ul', items: ['Methods are public abstract by default; variables are public static final.', 'A class uses implements; interfaces can extend multiple interfaces.', 'Cannot be instantiated.'] },
      { type: 'h', text: 'Functional interface' },
      { type: 'p', text: 'An interface with exactly ONE abstract method — used with lambda expressions.' },
      { type: 'code', code: '@FunctionalInterface\ninterface Calculator { int add(int a, int b); }\n\nCalculator c = (a, b) -> a + b;\nSystem.out.println(c.add(10, 20)); // 30' },
      { type: 'ul', items: ['Built-ins: Predicate.test(), Consumer.accept(), Supplier.get(), Function.apply().'] },
      { type: 'h', text: 'Diamond problem solved by interfaces' },
      { type: 'code', code: 'interface A { default void show() { System.out.println("A"); } }\ninterface B { default void show() { System.out.println("B"); } }\n\nclass C implements A, B {\n    public void show() { A.super.show(); } // resolve explicitly\n}' },
      { type: 'answer', text: 'Java resolves the diamond problem in interfaces by forcing the implementing class to override the conflicting method.' },
      { type: 'h', text: 'Default methods (Java 8)' },
      { type: 'ul', items: ['Allow adding methods to an interface without breaking existing implementations.', 'If a class and interface both define the method, the class wins.'] },
      { type: 'h', text: 'Java 8 features' },
      { type: 'ul', items: ['Lambda expressions.', 'Functional interfaces.', 'Stream API.', 'Default & static methods in interfaces.', 'Optional class.', 'New Date & Time API.', 'Method references (System.out::println).'] },
      {
        type: 'qa',
        items: [
          { q: 'Can an interface have a constructor?', a: 'No.' },
          { q: 'Can a lambda work without a functional interface?', a: 'No.' },
          { q: 'Can an interface extend another interface?', a: 'Yes — even multiple.' },
        ],
      },
    ],
  },
  {
    id: 'interface-types',
    title: 'Types of Interfaces',
    icon: 'grid',
    blocks: [
      { type: 'ul', items: ['Normal interface — more than one abstract method.', 'Functional interface — exactly one abstract method (Java 8, lambdas).', 'Marker interface — zero methods (Serializable, Cloneable, RandomAccess); gives metadata to JVM/frameworks.', 'Nested interface — declared inside a class or another interface.', 'Interface with default / static methods (Java 8).'] },
      {
        type: 'qa',
        items: [
          { q: 'Can an interface have zero abstract methods?', a: 'Yes — a marker interface.' },
          { q: 'Can an interface extend a class?', a: 'No.' },
          { q: 'Can a class implement multiple interfaces?', a: 'Yes.' },
        ],
      },
      { type: 'tip', text: 'Safest answer to "how many types?": Normal, Functional and Marker — plus Java 8 enhanced interfaces with default and static methods.' },
    ],
  },
  {
    id: 'static',
    title: 'The static Keyword',
    icon: 'bolt',
    blocks: [
      { type: 'answer', text: 'The static keyword is used to create class-level members that are shared among all objects of the class.' },
      { type: 'ul', items: ['Static variables: one copy per class, stored in the Method Area.', 'Static methods: called without an object; can only access static members; cannot use this/super.', 'Static block: runs once when the class is loaded (e.g. to initialise static data).', 'Top-level classes cannot be static; nested classes can.'] },
      { type: 'code', code: 'class Demo {\n    static String college = "SPIT"; // shared\n    static { System.out.println("Static block runs on class load"); }\n    static void show() { System.out.println("Static method"); }\n}\nDemo.show();' },
      { type: 'h', text: 'Why is main() static?' },
      { type: 'p', text: 'So the JVM can call it without creating an object first.' },
      {
        type: 'qa',
        items: [
          { q: 'Can a static block access non-static variables?', a: 'No.' },
          { q: 'Can we overload static methods?', a: 'Yes.' },
          { q: 'Can static methods be overridden?', a: 'No — that is method hiding, resolved at compile time.' },
          { q: 'Why cannot static access non-static?', a: 'Static members exist without an object, but non-static members need one.' },
        ],
      },
    ],
  },
  {
    id: 'exceptions',
    title: 'Exception Handling',
    icon: 'info',
    blocks: [
      { type: 'answer', text: 'An exception is a runtime error that occurs during program execution and can be handled using Java\u2019s exception handling mechanism.' },
      { type: 'h', text: 'Hierarchy' },
      { type: 'code', code: 'Object\n └── Throwable\n     ├── Exception\n     │     ├── Checked (compile-time)\n     │     └── Unchecked (runtime)\n     └── Error' },
      {
        type: 'table',
        head: ['Exception', 'Error'],
        rows: [
          ['Can be handled', 'Cannot (usually) be handled'],
          ['Program logic', 'JVM/system level'],
          ['e.g. IOException', 'e.g. OutOfMemoryError'],
        ],
      },
      { type: 'ul', items: ['Checked (compile-time): IOException, SQLException, FileNotFoundException — must be handled or declared.', 'Unchecked (runtime): NullPointerException, ArithmeticException, ArrayIndexOutOfBoundsException.'] },
      { type: 'h', text: 'Keywords & flow' },
      { type: 'ul', items: ['try — risky code.', 'catch — handles the exception (child before parent).', 'finally — always runs (cleanup).', 'throw — explicitly raise an exception.', 'throws — declare exceptions to the caller.'] },
      { type: 'code', code: 'try (FileReader fr = new FileReader("a.txt")) { // try-with-resources (Java 7+)\n    // use file — auto-closed, no finally needed\n} catch (IOException e) {\n    System.out.println(e.getMessage());\n}' },
      { type: 'h', text: 'Custom exception' },
      { type: 'code', code: 'class InvalidAgeException extends Exception {\n    InvalidAgeException(String msg) { super(msg); }\n}\nif (age < 18) throw new InvalidAgeException("Not eligible");' },
      {
        type: 'qa',
        items: [
          { q: 'Can finally be skipped?', a: 'Yes — with System.exit().' },
          { q: 'try without catch?', a: 'Yes, if finally is present.' },
          { q: 'Can we catch Error?', a: 'Technically yes, but it is not recommended.' },
        ],
      },
    ],
  },
  {
    id: 'throw-throws',
    title: 'try-catch-finally & throw vs throws',
    icon: 'info',
    blocks: [
      { type: 'h', text: 'Tricky finally behaviours' },
      { type: 'code', code: 'static int test() {\n    try { return 10; }\n    finally { return 20; } // finally OVERRIDES the return\n}\n// returns 20' },
      { type: 'ul', items: ['finally always runs — except System.exit() or JVM crash.', 'A return in finally overrides the return in try.', 'An exception in finally suppresses the exception from try.', 'Child exception must be caught before the parent (else compile error).'] },
      { type: 'h', text: 'throw vs throws' },
      {
        type: 'table',
        head: ['Feature', 'throw', 'throws'],
        rows: [
          ['Used for', 'Actually throw an exception', 'Declare an exception'],
          ['Position', 'Inside a method', 'Method signature'],
          ['Count', 'One at a time', 'Multiple allowed'],
          ['Use case', 'Custom / business logic', 'Delegate handling to caller'],
        ],
      },
      { type: 'code', code: 'if (age < 18) throw new InvalidAgeException("Not allowed"); // throw\n\nvoid read() throws IOException { /* ... */ }               // throws' },
      { type: 'answer', text: 'throw is used to explicitly raise an exception, while throws is used to declare and delegate exception-handling responsibility to the caller.' },
      {
        type: 'qa',
        items: [
          { q: 'throw without throws?', a: 'Yes — for unchecked exceptions.' },
          { q: 'throws without throw?', a: 'Yes — declaring a checked exception.' },
          { q: 'throw a checked exception without throws?', a: 'No — it must be declared or handled.' },
        ],
      },
    ],
  },
  {
    id: 'gc',
    title: 'Garbage Collection (deep)',
    icon: 'reset',
    blocks: [
      { type: 'answer', text: 'Garbage Collection is an automatic memory-management process where the JVM identifies and deletes unreachable objects to free heap memory.' },
      { type: 'h', text: 'Heap structure (GC works here)' },
      { type: 'code', code: 'Heap\n ├── Young Generation\n │     ├── Eden\n │     ├── Survivor S0\n │     └── Survivor S1\n └── Old Generation' },
      { type: 'ul', items: ['Object lifecycle: created in Eden → survives → Survivor → long-lived → Old Gen → unreachable → collected.', 'Reachability: an object is eligible when no GC Root references it (local vars, static vars, active threads, JNI refs).'] },
      { type: 'h', text: 'Types of GC' },
      { type: 'ul', items: ['Minor GC — Young Generation, fast.', 'Major GC — Old Generation, slower.', 'Full GC — whole heap, expensive (Stop-The-World pause).'] },
      { type: 'h', text: 'Algorithms & collectors' },
      { type: 'ul', items: ['Mark & Sweep; Mark-Sweep-Compact (fixes fragmentation); Copying (Young Gen); Generational.', 'Collectors: Serial, Parallel, CMS, G1 (default Java 9+), ZGC (ultra-low latency).'] },
      {
        type: 'qa',
        items: [
          { q: 'Is an object deleted immediately when eligible?', a: 'No — the GC decides the timing.' },
          { q: 'Can we force GC?', a: 'System.gc() only requests it; the JVM may ignore it.' },
          { q: 'Does GC collect cyclic references?', a: 'Yes — Java GC uses reachability, not reference counting.' },
          { q: 'Is finalize() reliable?', a: 'No — it is unreliable and deprecated (Java 9+).' },
        ],
      },
      { type: 'tip', text: 'To sound senior: mention heap generations, Stop-The-World, generational algorithms, and G1 GC.' },
    ],
  },
  {
    id: 'multithreading',
    title: 'Multithreading (complete)',
    icon: 'route',
    blocks: [
      { type: 'h', text: 'Process vs Thread' },
      { type: 'answer', text: 'A process is independent and has its own memory, whereas threads are lightweight and share memory within a process.' },
      { type: 'answer', text: 'Multithreading allows multiple threads to run concurrently within a single process, improving CPU utilization.' },
      { type: 'h', text: 'Creating a thread' },
      { type: 'code', code: '// 1) Extend Thread\nclass MyThread extends Thread { public void run() { System.out.println("run"); } }\nnew MyThread().start();\n\n// 2) Implement Runnable (preferred — Java has single inheritance)\nclass MyTask implements Runnable { public void run() { System.out.println("run"); } }\nnew Thread(new MyTask()).start();' },
      { type: 'ul', items: ['start() creates a new thread; run() is just a normal method call.', 'Lifecycle: New → Runnable → Running → Waiting/Blocked → Terminated.'] },
      { type: 'h', text: 'sleep vs wait vs join' },
      {
        type: 'table',
        head: ['Method', 'sleep()', 'wait()', 'join()'],
        rows: [
          ['Releases lock', 'No', 'Yes', 'No'],
          ['Belongs to', 'Thread', 'Object', 'Thread'],
          ['Purpose', 'Pause', 'Communication', 'Wait for a thread to finish'],
        ],
      },
      { type: 'ul', items: ['Thread priority: 1 (MIN) to 10 (MAX), 5 = NORM — only a hint to the scheduler.', 'Daemon thread: background thread that dies when the main thread ends (e.g. the GC).'] },
      { type: 'h', text: 'Synchronization & locks' },
      { type: 'code', code: 'synchronized void withdraw() { /* one thread at a time */ }\n\nsynchronized (obj) { /* synchronized block */ }' },
      { type: 'ul', items: ['Prevents data inconsistency / race conditions.', 'Locks: intrinsic (monitor) lock via synchronized; explicit ReentrantLock.'] },
      { type: 'h', text: 'Thread communication' },
      { type: 'ul', items: ['wait() — pause & release lock; notify() — wake one thread; notifyAll() — wake all.', 'Classic use: Producer–Consumer.'] },
      { type: 'h', text: 'Thread pool (Executor framework)' },
      { type: 'code', code: 'ExecutorService service = Executors.newFixedThreadPool(5);\nservice.execute(() -> System.out.println("Task running"));\nservice.shutdown();' },
      { type: 'answer', text: 'Thread pooling improves performance by reusing threads instead of creating new ones for every task.' },
      {
        type: 'qa',
        items: [
          { q: 'Why start() and not run()?', a: 'start() spawns a new thread; run() executes on the current thread.' },
          { q: 'Does sleep() release the lock?', a: 'No; wait() does.' },
          { q: 'Are static variables thread-safe?', a: 'No — they are shared among threads.' },
        ],
      },
    ],
  },
  {
    id: 'deadlock',
    title: 'Deadlock: Mutual Exclusion & Hold-and-Wait',
    icon: 'lock',
    blocks: [
      { type: 'p', text: 'A deadlock needs all four conditions together: Mutual Exclusion, Hold and Wait, No Preemption, and Circular Wait.' },
      { type: 'h', text: 'Mutual Exclusion' },
      { type: 'answer', text: 'Mutual exclusion is a condition where a shared resource cannot be accessed by more than one thread at the same time.' },
      { type: 'ul', items: ['Analogy: a bathroom used by one person at a time; the door lock = mutual exclusion.', 'Enforced in Java via synchronized / monitor locks.', 'Prevents race conditions, but alone does not cause a deadlock.'] },
      { type: 'h', text: 'Hold and Wait' },
      { type: 'answer', text: 'Hold and wait is a condition where a thread holds one resource while waiting for additional resources — without releasing what it already holds.' },
      { type: 'code', code: 'Thread-1: holds Lock A → waits for Lock B\nThread-2: holds Lock B → waits for Lock A   // deadlock' },
      { type: 'ul', items: ['Prevention: acquire all needed locks at once (all-or-nothing), or release held resources before requesting new ones.'] },
      {
        type: 'qa',
        items: [
          { q: 'Can deadlock occur without mutual exclusion?', a: 'No.' },
          { q: 'Can deadlock occur without hold and wait?', a: 'No.' },
          { q: 'Which is more dangerous?', a: 'Hold and wait — it blocks progress by locking resources while waiting.' },
        ],
      },
    ],
  },
  {
    id: 'collections',
    title: 'Collection Framework',
    icon: 'layers',
    blocks: [
      { type: 'answer', text: 'The Java Collection Framework provides a unified architecture to store, retrieve, and manipulate a group of objects efficiently.' },
      { type: 'code', code: 'Iterable → Collection → List / Set / Queue\nMap (separate hierarchy — key/value)' },
      { type: 'h', text: 'List (ordered, allows duplicates)' },
      {
        type: 'table',
        head: ['Feature', 'ArrayList', 'LinkedList'],
        rows: [
          ['Structure', 'Dynamic array', 'Doubly linked list'],
          ['Access', 'Fast', 'Slow'],
          ['Insertion/deletion', 'Slow', 'Fast'],
          ['Memory', 'Less', 'More'],
        ],
      },
      { type: 'h', text: 'Set (unique elements)' },
      {
        type: 'table',
        head: ['Feature', 'HashSet', 'TreeSet'],
        rows: [
          ['Order', 'Unordered', 'Sorted'],
          ['Null', 'One allowed', 'Not allowed'],
          ['Performance', 'Faster', 'Slower'],
        ],
      },
      { type: 'h', text: 'Map (key–value)' },
      {
        type: 'table',
        head: ['Feature', 'HashMap', 'Hashtable'],
        rows: [
          ['Thread-safe', 'No', 'Yes'],
          ['Null keys', 'One', 'None'],
          ['Performance', 'Fast', 'Slow'],
        ],
      },
      { type: 'h', text: 'Other must-knows' },
      { type: 'ul', items: ['Iterator (forward only, any collection) vs ListIterator (both directions, List only).', 'Fail-fast (ConcurrentModificationException, e.g. ArrayList) vs Fail-safe (e.g. CopyOnWriteArrayList).', 'Comparable (natural order, compareTo) vs Comparator (custom order, compare).', 'Concurrent collections: ConcurrentHashMap, CopyOnWriteArrayList.'] },
      {
        type: 'qa',
        items: [
          { q: 'Why does Map not extend Collection?', a: 'Because it stores key-value pairs, not single elements.' },
          { q: 'Which collection is fastest for lookup?', a: 'HashMap / HashSet — average O(1).' },
          { q: 'Can we store null in a HashMap?', a: 'Yes — one null key and multiple null values.' },
        ],
      },
    ],
  },
  {
    id: 'equals-hashcode',
    title: 'equals(), == & hashCode()',
    icon: 'check',
    blocks: [
      { type: 'p', text: 'A favourite trap. == compares references (or primitive values); equals() compares content (when overridden). If you override equals(), you MUST override hashCode().' },
      {
        type: 'table',
        head: ['Aspect', '== operator', 'equals() method'],
        rows: [
          ['Compares', 'References (memory address) for objects', 'Content / logical equality'],
          ['Type', 'Operator', 'Method (from Object)'],
          ['Overridable', 'No', 'Yes'],
        ],
      },
      { type: 'answer', text: '== checks if two references point to the same object, while equals() checks if two objects are logically equal. For example, two different String objects with the same text are == false but equals() true (unless interned).' },
      { type: 'h', text: 'The equals–hashCode contract' },
      { type: 'ul', items: ['If a.equals(b) is true, then a.hashCode() == b.hashCode() must be true.', 'Equal hashCodes do NOT require equal objects (collisions are allowed).', 'Breaking this contract corrupts HashMap/HashSet — objects get lost or duplicated.'] },
      {
        type: 'qa',
        items: [
          { q: 'What happens if you override equals but not hashCode?', a: 'Two equal objects may land in different buckets, so HashMap/HashSet lookups fail — you can put a key in and never get it back.' },
          { q: 'Is it valid for hashCode() to always return the same constant?', a: 'Technically legal, but it degrades a HashMap to O(n) since everything collides into one bucket.' },
        ],
      },
    ],
  },
  {
    id: 'final-finally-finalize',
    title: 'final vs finally vs finalize',
    icon: 'lock',
    blocks: [
      {
        type: 'table',
        head: ['Keyword', 'What it is', 'Purpose'],
        rows: [
          ['final', 'Modifier', 'Constant variable, non-overridable method, non-inheritable class.'],
          ['finally', 'Block', 'Always executes after try/catch — used for cleanup.'],
          ['finalize', 'Method', 'Called by GC before object destruction (deprecated, avoid).'],
        ],
      },
      { type: 'answer', text: 'final is a keyword to make a variable constant, a method non-overridable, or a class non-inheritable. finally is a block that always runs after try-catch for cleanup. finalize is a method the garbage collector may call before reclaiming an object — but it\'s unreliable and deprecated.' },
      {
        type: 'qa',
        items: [
          { q: 'When does finally NOT execute?', a: 'If the JVM exits (System.exit()), the thread is killed, or an infinite loop/hardware failure occurs.' },
          { q: 'Does finally run if try has a return?', a: 'Yes — finally runs before the method actually returns.' },
        ],
      },
    ],
  },
  {
    id: 'pass-by-value',
    title: 'Pass by value or reference?',
    icon: 'route',
    blocks: [
      { type: 'answer', text: 'Java is strictly pass-by-value. For objects, the value passed is a copy of the reference — so you can mutate the object\'s fields (visible to the caller), but reassigning the parameter to a new object does NOT affect the caller.' },
      { type: 'ul', items: ['Primitives: a copy of the value is passed — changes don\'t affect the original.', 'Objects: a copy of the reference is passed — you can change the object\'s state, but not repoint the caller\'s variable.'] },
      { type: 'code', code: 'void change(int[] a, int x) {\n  a[0] = 99;   // visible to caller (same object)\n  x = 5;       // NOT visible (copy of value)\n}' },
      { type: 'tip', text: 'Say it precisely: "Java is always pass-by-value; for objects the value is the reference." This exact phrasing impresses interviewers.' },
    ],
  },
  {
    id: 'immutability',
    title: 'Immutability & Singleton',
    icon: 'bolt',
    blocks: [
      { type: 'h', text: 'Why are Strings immutable?' },
      { type: 'ul', items: ['Security — used in file paths, DB URLs, credentials that mustn\'t change under the hood.', 'String pool — safe sharing of literals requires immutability.', 'Thread safety — immutable objects are inherently thread-safe.', 'Caching hashCode — computed once, reused (great for HashMap keys).'] },
      { type: 'h', text: 'How to make an immutable class' },
      { type: 'ul', items: ['Mark the class final (no subclassing).', 'Make all fields private and final.', 'No setters; initialise via constructor.', 'Return deep copies of mutable fields from getters.'] },
      { type: 'h', text: 'Singleton pattern' },
      { type: 'answer', text: 'A singleton ensures a class has only one instance with a global access point. Thread-safe implementations include eager initialisation, double-checked locking with a volatile field, or the preferred Bill Pugh static-inner-class holder idiom (lazy + thread-safe without synchronisation).' },
      { type: 'code', code: 'class Singleton {\n  private Singleton() {}\n  private static class Holder { static final Singleton I = new Singleton(); }\n  public static Singleton getInstance() { return Holder.I; }\n}' },
    ],
  },
  {
    id: 'cloning',
    title: 'Shallow vs deep copy',
    icon: 'layers',
    blocks: [
      {
        type: 'table',
        head: ['Aspect', 'Shallow copy', 'Deep copy'],
        rows: [
          ['Nested objects', 'Shares references', 'Duplicates recursively'],
          ['Independence', 'Changes leak between copies', 'Fully independent'],
          ['Cost', 'Cheap', 'Expensive'],
          ['Default clone()', 'Yes (shallow)', 'Manual implementation'],
        ],
      },
      { type: 'answer', text: 'A shallow copy duplicates the top-level object but shares references to nested objects, so mutating a nested object affects both copies. A deep copy recursively duplicates everything, making the copies fully independent.' },
      {
        type: 'qa',
        items: [
          { q: 'How do you achieve a deep copy?', a: 'Override clone() to copy nested objects, use a copy constructor, or serialise/deserialise the object.' },
          { q: 'What is the marker interface Cloneable?', a: 'An empty interface that signals Object.clone() is allowed; without it clone() throws CloneNotSupportedException.' },
        ],
      },
    ],
  },
  {
    id: 'keywords-misc',
    title: 'Keywords & rapid fire',
    icon: 'sparkles',
    blocks: [
      {
        type: 'table',
        head: ['Keyword', 'Meaning'],
        rows: [
          ['this', 'Reference to the current object.'],
          ['super', 'Reference to the immediate parent class.'],
          ['transient', 'Field is skipped during serialization.'],
          ['volatile', 'Reads/writes go straight to main memory (visibility across threads).'],
          ['synchronized', 'Only one thread executes the block/method at a time.'],
          ['static', 'Belongs to the class, not an instance.'],
        ],
      },
      {
        type: 'levels',
        items: [
          { level: 'Easy', q: 'What is autoboxing?', a: 'Automatic conversion between primitives and their wrapper classes, e.g. int ↔ Integer. Unboxing is the reverse.' },
          { level: 'Easy', q: 'What is a marker interface?', a: 'An empty interface (no methods) that tags a class with metadata, e.g. Serializable, Cloneable.' },
          { level: 'Medium', q: 'String vs StringBuffer vs StringBuilder?', a: 'String is immutable. StringBuffer is mutable and thread-safe (synchronized, slower). StringBuilder is mutable and not thread-safe (faster) — use it for heavy single-threaded string building.' },
          { level: 'Medium', q: 'Comparable vs Comparator?', a: 'Comparable defines a natural ordering via compareTo() inside the class. Comparator defines external, custom orderings via compare() — you can have many.' },
          { level: 'Medium', q: 'volatile vs synchronized?', a: 'volatile guarantees visibility of a single variable across threads but not atomicity. synchronized guarantees both visibility and atomicity (mutual exclusion) for a block.' },
          { level: 'Hard', q: 'Why is char[] preferred over String for passwords?', a: 'Strings are immutable and live in the pool, lingering in memory until GC — you can\'t erase them. A char[] can be overwritten (zeroed) immediately after use.' },
          { level: 'Hard', q: 'How does HashMap work internally?', a: 'It\'s an array of buckets; the key\'s hashCode is hashed to a bucket index. Collisions form a linked list, which converts to a balanced tree (red-black) after 8 entries in Java 8+ for O(log n) worst case. It resizes (rehashes) past the load factor (0.75).' },
        ],
      },
    ],
  },,
    {
      id: "ib-fresher",
      title: "InterviewBit — Freshers",
      icon: "sparkles",
      blocks: [
        { type: 'p', text: 'Questions curated from InterviewBit (interviewbit.com), with concise, interview-ready answers.' },
        {
          type: 'qa',
          items: [
            { q: "Why is Java a platform independent language?", a: "Java language was developed so that it does not depend on any hardware or software because the compiler compiles the code and then converts it to platform-independent byte code which can be run on multiple systems. - The only condition to run that byte code is for the machine to have a runtime environment (JRE) installed in it." },
            { q: "Why is Java not a pure object oriented language?", a: "Java supports primitive data types - byte, boolean, char, short, int, float, long, and double and hence it is not a pure object oriented language." },
            { q: "Difference between Heap and Stack Memory in java. And how java utilizes this.", a: "Stack memory is the portion of memory that was assigned to every individual program. And it was fixed. On the other hand, Heap memory is the portion that was not allocated to the java program but it will be available for use by the java program when it is required, mostly during the runtime of the program. Java Utilizes this memory as - - When we write a java program then all the variables, methods, etc are stored in the stack memory. - And when we create any object in the java program then that object was created in the heap memory. And it was referenced from the stack memory. Example- Consider the below java program: class Main { public void printArray(int[] array){ for(int i : array) System.out.println(i); } public static void…" },
            { q: "Can java be said to be the complete object-oriented programming language?", a: "It is not wrong if we claim that Java is the complete object-oriented programming language because everything in Java is under the classes and we can access them by creating the objects. But we can even say that Java is not a completely object-oriented programming language because it has the support of primitive data types like int, float, char, boolean, double, etc. Now for the question: Is Java a completely object-oriented programming language? We can say that - Java is not a pure object-oriented programming language, because it has direct access to primitive data types. And these primitive data types don't directly belong to the Integer classes." },
            { q: "How is Java different from C++?", a: "- C++ is only a compiled language, whereas Java is compiled as well as an interpreted language. - Java programs are machine-independent whereas a c++ program can run only in the machine in which it is compiled. - C++ allows users to use pointers in the program. Whereas java doesn’t allow it. Java internally uses pointers. - C++ supports the concept of Multiple inheritances whereas Java doesn't support this. And it is due to avoiding the complexity of name ambiguity that causes the diamond problem." },
            { q: "Pointers are used in C/ C++. Why does Java not make use of pointers?", a: "Pointers are quite complicated and unsafe to use by beginner programmers. Java focuses on code simplicity, and the usage of pointers can make it challenging. Pointer utilization can also cause potential errors. Moreover, security is also compromised if pointers are used because the users can directly access memory with the help of pointers. Thus, a certain level of abstraction is furnished by not including pointers in Java. Moreover, the usage of pointers can make the procedure of garbage collection quite slow and erroneous. Java makes use of references as these cannot be manipulated, unlike pointers." },
            { q: "What do you understand by an instance variable and a local variable?", a: "Instance variables are those variables that are accessible by all the methods in the class. They are declared outside the methods and inside the class. These variables describe the properties of an object and remain bound to it at any cost. All the objects of the class will have their copy of the variables for utilization. If any modification is done on these variables, then only that instance will be impacted by it, and all other class instances continue to remain unaffected. Example: class Athlete { public String athleteName; public double athleteSpeed; public int athleteAge; } Local variables are those variables present within a block, function, or constructor and can be accessed only inside them. The utilization of the variable is…" },
            { q: "What are the default values assigned to variables and instances in java?", a: "- There are no default values assigned to the variables in java. We need to initialize the value before using it. Otherwise, it will throw a compilation error of (Variable might not be initialized). - But for instance, if we create the object, then the default value will be initialized by the default constructor depending on the data type. - If it is a reference, then it will be assigned to null. - If it is numeric, then it will assign to 0. - If it is a boolean, then it will be assigned to false. Etc." },
            { q: "What do you mean by data encapsulation?", a: "- Data Encapsulation is an Object-Oriented Programming concept of hiding the data attributes and their behaviours in a single unit. - It helps developers to follow modularity while developing software by ensuring that each object is independent of other objects by having its own methods, attributes, and functionalities. - It is used for the security of the private properties of an object and hence serves the purpose of data hiding." },
            { q: "Tell us something about JIT compiler.", a: "- JIT stands for Just-In-Time and it is used for improving the performance during run time. It does the task of compiling parts of byte code having similar functionality at the same time thereby reducing the amount of compilation time for the code to run. - The compiler is nothing but a translator of source code to machine-executable code. But what is special about the JIT compiler? Let us see how it works: - - First, the Java source code (.java) conversion to byte code (.class) occurs with the help of the javac compiler. - Then, the .class files are loaded at run time by JVM and with the help of an interpreter, these are converted to machine understandable code. - JIT compiler is a part of JVM. When the JIT compiler is enabled, the JVM…" },
            { q: "Can you tell the difference between equals() method and equality operator (==) in Java?", a: "We are already aware of the (==) equals operator. That we have used this to compare the equality of the values. But when we talk about the terms of object-oriented programming, we deal with the values in the form of objects. And this object may contain multiple types of data. So using the (==) operator does not work in this case. So we need to go with the .equals() method. Both [(==) and .equals()] primary functionalities are to compare the values, but the secondary functionality is different. So in order to understand this better, let’s consider this with the example - String str1 = \"InterviewBit\"; String str2 = \"InterviewBit\"; System.out.println(str1 == str2); This code will print true. We know that both strings are equals so it will…" },
            { q: "How is an infinite loop declared in Java?", a: "Infinite loops are those loops that run infinitely without any breaking conditions. Some examples of consciously declaring infinite loop is: - Using For Loop: for (;;) { // Business logic // Any break logic } - Using while loop: while(true){ // Business logic // Any break logic } - Using do-while loop: do{ // Business logic // Any break logic }while(true);" },
            { q: "Briefly explain the concept of constructor overloading", a: "Constructor overloading is the process of creating multiple constructors in the class consisting of the same name with a difference in the constructor parameters. Depending upon the number of parameters and their corresponding types, distinguishing of the different types of constructors is done by the compiler. class Hospital { int variable1, variable2; double variable3; public Hospital(int doctors, int nurses) { variable1 = doctors; variable2 = nurses; } public Hospital(int doctors) { variable1 = doctors; } public Hospital(double salaries) { variable3 = salaries } } Three constructors are defined here but they differ on the basis of parameter type and their numbers." },
            { q: "Define Copy constructor in java.", a: "Copy Constructor is the constructor used when we want to initialize the value to the new object from the old object of the same class. class InterviewBit{ String department; String service; InterviewBit(InterviewBit ib){ this.departments = ib.departments; this.services = ib.services; } } Here we are initializing the new object value from the old object value in the constructor. Although, this can also be achieved with the help of object cloning." },
            { q: "Can the main method be Overloaded?", a: "Yes, It is possible to overload the main method. We can create as many overloaded main methods we want. However, JVM has a predefined calling method that JVM will only call the main method with the definition of - public static void main(string[] args) Consider the below code snippets: class Main { public static void main(String args[]) { System.out.println(\" Main Method\"); } public static void main(int[] args){ System.out.println(\"Overloaded Integer array Main Method\"); } public static void main(char[] args){ System.out.println(\"Overloaded Character array Main Method\"); } public static void main(double[] args){ System.out.println(\"Overloaded Double array Main Method\"); } public static void main(float args){ System.out.println(\"Overloaded…" },
            { q: "Comment on method overloading and overriding by citing relevant examples.", a: "In Java, method overloading is made possible by introducing different methods in the same class consisting of the same name. Still, all the functions differ in the number or type of parameters. It takes place inside a class and enhances program readability. The only difference in the return type of the method does not promote method overloading. The following example will furnish you with a clear picture of it. class OverloadingHelp { public int findarea (int l, int b) { int var1; var1 = l * b; return var1; } public int findarea (int l, int b, int h) { int var2; var2 = l * b * h; return var2; } } Both the functions have the same name but differ in the number of arguments. The first method calculates the area of the rectangle, whereas the…" },
            { q: "A single try block and multiple catch blocks can co-exist in a Java Program. Explain.", a: "Yes, multiple catch blocks can exist but specific approaches should come prior to the general approach because only the first catch block satisfying the catch condition is executed. The given code illustrates the same: public class MultipleCatch { public static void main(String args[]) { try { int n = 1000, x = 0; int arr[] = new int[n]; for (int i = 0; i <= n; i++) { arr[i] = i / x; } } catch (ArrayIndexOutOfBoundsException exception) { System.out.println(\"1st block = ArrayIndexOutOfBoundsException\"); } catch (ArithmeticException exception) { System.out.println(\"2nd block = ArithmeticException\"); } catch (Exception exception) { System.out.println(\"3rd block = Exception\"); } } } Here, the second catch block will be executed because of…" },
            { q: "Explain the use of final keyword in variable, method and class.", a: "In Java, the final keyword is used as defining something as constant /final and represents the non-access modifier. - final variable: - - When a variable is declared as final in Java, the value can’t be modified once it has been assigned. - If any value has not been assigned to that variable, then it can be assigned only by the constructor of the class. - final method: - - A method declared as final cannot be overridden by its children's classes. - A constructor cannot be marked as final because whenever a class is inherited, the constructors are not inherited. Hence, marking it final doesn't make sense. Java throws compilation error saying -modifier final not allowed here - final class: - - No classes can be inherited from the class…" },
            { q: "Do final, finally and finalize keywords have the same function?", a: "All three keywords have their own utility while programming. Final: If any restriction is required for classes, variables, or methods, the final keyword comes in handy. Inheritance of a final class and overriding of a final method is restricted by the use of the final keyword. The variable value becomes fixed after incorporating the final keyword. Example: final int a=100; a = 0; // error The second statement will throw an error. Finally: It is the block present in a program where all the codes written inside it get executed irrespective of handling of exceptions. Example: try { int variable = 5; } catch (Exception exception) { System.out.println(\"Exception occurred\"); } finally { System.out.println(\"Execution of finally block\"); }…" },
            { q: "Is it possible that the ‘finally’ block will not be executed? If yes then list the case.", a: "Yes. It is possible that the ‘finally’ block will not be executed. The cases are- - Suppose we use System.exit() in the above statement. - If there are fatal errors like Stack overflow, Memory access error, etc." },
            { q: "Identify the output of the java program and state the reason.", a: "1. public class InterviewBit 2. { 3. public static void main(String[] args) { 4. final int i; 5. i = 20; 6. int j = i+20; 7. i = j+30; 8. System.out.println(i + \" \" + j); 9. } 10. } The above code will generate a compile-time error at Line 7 saying - [error: variable i might already have been initialized]. It is because variable ‘i’ is the final variable. And final variables are allowed to be initialized only once, and that was already done on line no 5." },
            { q: "When can you use super keyword?", a: "- The super keyword is used to access hidden fields and overridden methods or attributes of the parent class. - Following are the cases when this keyword can be used: - - Accessing data members of parent class when the member names of the class and its child subclasses are same. - To call the default and parameterized constructor of the parent class inside the child class. - Accessing the parent class methods when the child classes have overridden them. - The following example demonstrates all 3 cases when a super keyword is used. class Parent{ protected int num = 1; Parent(){ System.out.println(\"Parent class default constructor.\"); } Parent(String x){ System.out.println(\"Parent class parameterised constructor.\"); } public void foo(){…" },
            { q: "Can the static methods be overloaded?", a: "Yes! There can be two or more static methods in a class with the same name but differing input parameters." },
            { q: "Why is the main method static in Java?", a: "The main method is always static because static members are those methods that belong to the classes, not to an individual object. So if the main method will not be static then for every object, It is available. And that is not acceptable by JVM. JVM calls the main method based on the class name itself. Not by creating the object. Because there must be only 1 main method in the java program as the execution starts from the main method. So for this reason the main method is static." },
            { q: "Can the static methods be overridden?", a: "- No! Declaration of static methods having the same signature can be done in the subclass but run time polymorphism can not take place in such cases. - Overriding or dynamic polymorphism occurs during the runtime, but the static methods are loaded and looked up at the compile time statically. Hence, these methods cant be overridden." },
            { q: "Difference between static methods, static variables, and static classes in java.", a: "- Static Methods and Static variables are those methods and variables that belong to the class of the java program, not to the object of the class. This gets memory where the class is loaded. And these can directly be called with the help of class names. - - For example - We have used mathematical functions in the java program like - max(), min(), sqrt(), pow(), etc. And if we notice that, then we will find that we call it directly with the class name. Like - Math.max(), Math.min(), etc. So that is a static method. And Similarly static variables we have used like (length) for the array to get the length. So that is the static method. - Static classes - A class in the java program cannot be static except if it is the inner class. If it is…" },
            { q: "What is the main objective of garbage collection?", a: "The main objective of this process is to free up the memory space occupied by the unnecessary and unreachable objects during the Java program execution by deleting those unreachable objects. - This ensures that the memory resource is used efficiently, but it provides no guarantee that there would be sufficient memory for the program execution." },
            { q: "What is a ClassLoader?", a: "- Java Classloader is the program that belongs to JRE (Java Runtime Environment). The task of ClassLoader is to load the required classes and interfaces to the JVM when required. - Example- To get input from the console, we require the scanner class. And the Scanner class is loaded by the ClassLoader." },
            { q: "What are shallow copy and deep copy in java?", a: "To copy the object's data, we have several methods like deep copy and shallow copy. Example - class Rectangle{ int length = 5; int breadth = 3; } Object for this Rectangle class - Rectangle obj1 = new Rectangle(); - Shallow copy - The shallow copy only creates a new reference and points to the same object. Example - For Shallow copy, we can do this by - Rectangle obj2 = obj1; Now by doing this what will happen is the new reference is created with the name obj2 and that will point to the same memory location. - Deep Copy - In a deep copy, we create a new object and copy the old object value to the new object. Example - Rectangle obj3 = new Rectangle(); Obj3.length = obj1.length; Obj3.breadth = obj1.breadth; Both these objects will point to…" }
          ],
        },
      ],
    },
    {
      id: "ib-intermediate",
      title: "InterviewBit — Intermediate",
      icon: "layers",
      blocks: [
        { type: 'p', text: 'Questions curated from InterviewBit (interviewbit.com), with concise, interview-ready answers.' },
        {
          type: 'qa',
          items: [
            { q: "Apart from the security aspect, what are the reasons behind making strings immutable in Java?", a: "A String is made immutable due to the following reasons: - String Pool: Designers of Java were aware of the fact that String data type is going to be majorly used by the programmers and developers. Thus, they wanted optimization from the beginning. They came up with the notion of using the String pool (a storage area in Java heap) to store the String literals. They intended to decrease the temporary String object with the help of sharing. An immutable class is needed to facilitate sharing. The sharing of the mutable structures between two unknown parties is not possible. Thus, immutable Java String helps in executing the concept of String Pool. - Multithreading: The safety of threads regarding the String objects is an important aspect in…" },
            { q: "What is a singleton class in Java? And How to implement a singleton class?", a: "Singleton classes are those classes, whose objects are created only once. And with only that object the class members can be accessed. Understand this with the help of an example-: Consider the water jug in the office and if every employee wants that water then they will not create a new water jug for drinking water. They will use the existing one with their own reference as a glass. So programmatically it should be implemented as - class WaterJug{ private int waterQuantity = 500; private WaterJug(){} private WaterJug object = null; // Method to provide the service of Giving Water. public int getWater(int quantity){ waterQuantity -= quantity; return quantity; } // Method to return the object to the user. public static Waterjug…" },
            { q: "Which of the below generates a compile-time error? State the reason.", a: "1. int[] n1 = new int[0]; 2. boolean[] n2 = new boolean[-200]; 3. double[] n3 = new double[2241423798]; 4. char[] ch = new char[20]; We get a compile-time error in line 3. The error we will get in Line 3 is - integer number too large. It is because the array requires size as an integer. And Integer takes 4 Bytes in the memory. And the number (2241423798) is beyond the capacity of the integer. The maximum array size we can declare is - (2147483647). Because the array requires the size in integer, none of the lines (1, 2, and 4) will give a compile-time error. The program will compile fine. But we get the runtime exception in line 2. The exception is - NegativeArraySizeException. Here what will happen is - At the time when JVM will allocate…" },
            { q: "How would you differentiate between a String, StringBuffer, and a StringBuilder?", a: "- Storage area: In string, the String pool serves as the storage area. For StringBuilder and StringBuffer, heap memory is the storage area. - Mutability: A String is immutable, whereas both the StringBuilder and StringBuffer are mutable. - Efficiency: It is quite slow to work with a String. However, StringBuilder is the fastest in performing operations. The speed of a StringBuffer is more than a String and less than a StringBuilder. (For example appending a character is fastest in StringBuilder and very slow in String because a new memory is required for the new String with appended character.) - Thread-safe: In the case of a threaded environment, StringBuilder and StringBuffer are used whereas a String is not used. However, StringBuilder…" },
            { q: "Using relevant properties highlight the differences between interfaces and abstract classes.", a: "- Availability of methods: Only abstract methods are available in interfaces, whereas non-abstract methods can be present along with abstract methods in abstract classes. - Variable types: Static and final variables can only be declared in the case of interfaces, whereas abstract classes can also have non-static and non-final variables. - Inheritance: Multiple inheritances are facilitated by interfaces, whereas abstract classes do not promote multiple inheritances. - Data member accessibility: By default, the class data members of interfaces are of the public- type. Conversely, the class members for an abstract class can be protected or private also. - Implementation: With the help of an abstract class, the implementation of an interface…" },
            { q: "Is this program giving a compile-time error? If Yes then state the reason and number of errors it will give. If not then state the reason.", a: "abstract final class InterviewBit{ 2. public abstract void printMessage(); 3. } 4. class ScalarAcademy extends InterviewBit{ 5. public void printMessage(){ 6. System.out.println(\"Welcome to Scalar Academy By InterviewBit\"); 7. } 8. } 9. class ScalarTopics extends ScalarAcademy{ 10. public void printMessage(){ 11. System.out.println(\"Welcome to Scalar Topics By Scalar Academy\"); 12. } 13. } public class Main{ public static void main(String[] args) { InterviewBit ib = new ScalarTopics(); ib.printMessage(); } } The above program will give a compile-time error. The compiler will throw 2 errors in this. - [Illegal Combination of modifiers: abstract and final] at line 1. - [Cannot inherit from final ‘InterviewBit’] at line 4. It is because…" },
            { q: "What is a Comparator in java?", a: "Consider the example where we have an ArrayList of employees like( EId, Ename, Salary), etc. Now if we want to sort this list of employees based on the names of employees. Then that is not possible to sort using the Collections.sort() method. We need to provide something to the sort() function depending on what values we have to perform sorting. Then in that case a comparator is used. Comparator is the interface in java that contains the compare method. And by overloading the compare method, we can define that on what basis we need to compare the values." },
            { q: "In Java, static as well as private method overriding is possible. Comment on the statement.", a: "The statement in the context is completely False. The static methods have no relevance with the objects, and these methods are of the class level. In the case of a child class, a static method with a method signature exactly like that of the parent class can exist without even throwing any compilation error. The phenomenon mentioned here is popularly known as method hiding, and overriding is certainly not possible. Private method overriding is unimaginable because the visibility of the private method is restricted to the parent class only. As a result, only hiding can be facilitated and not overriding." },
            { q: "What makes a HashSet different from a TreeSet?", a: "Although both HashSet and TreeSet are not synchronized and ensure that duplicates are not present, there are certain properties that distinguish a HashSet from a TreeSet. - Implementation: For a HashSet, the hash table is utilized for storing the elements in an unordered manner. However, TreeSet makes use of the red-black tree to store the elements in a sorted manner. - Complexity/ Performance: For adding, retrieving, and deleting elements, the time amortized complexity is O(1) for a HashSet. The time complexity for performing the same operations is a bit higher for TreeSet and is equal to O(log n). Overall, the performance of HashSet is faster in comparison to TreeSet. - Methods: hashCode() and equals() are the methods utilized by HashSet…" },
            { q: "Why is the character array preferred over string for storing confidential information?", a: "In Java, a string is basically immutable i.e. it cannot be modified. After its declaration, it continues to stay in the string pool as long as it is not removed in the form of garbage. In other words, a string resides in the heap section of the memory for an unregulated and unspecified time interval after string value processing is executed. As a result, vital information can be stolen for pursuing harmful activities by hackers if a memory dump is illegally accessed by them. Such risks can be eliminated by using mutable objects or structures like character arrays for storing any variable. After the work of the character array variable is done, the variable can be configured to blank at the same instant. Consequently, it helps in saving…" },
            { q: "What do we get in the JDK file?", a: "- JDK- For making java programs, we need some tools that are provided by JDK (Java Development Kit). JDK is the package that contains various tools, Compiler, Java Runtime Environment, etc. - JRE - To execute the java program we need an environment. (Java Runtime Environment) JRE contains a library of Java classes + JVM. What are JAVA Classes? It contains some predefined methods that help Java programs to use that feature, build and execute. For example - there is a system class in java that contains the print-stream method, and with the help of this, we can print something on the console. - JVM - (Java Virtual Machine) JVM is a part of JRE that executes the Java program at the end. Actually, it is part of JRE, but it is software that…" },
            { q: "What are the differences between JVM, JRE and JDK in Java?", a: "Criteria | JDK | JRE | JVM | Abbreviation | Java Development Kit | Java Runtime Environment | Java Virtual Machine | Definition | JDK is a complete software development kit for developing Java applications. It comprises JRE, JavaDoc, compiler, debuggers, etc. | JRE is a software package providing Java class libraries, JVM and all the required components to run the Java applications. | JVM is a platform-dependent, abstract machine comprising of 3 specifications - document describing the JVM implementation requirements, computer program meeting the JVM requirements and instance object for executing the Java byte code and provide the runtime environment for execution. | Main Purpose | JDK is mainly used for code development and execution. |…" },
            { q: "What are the differences between HashMap and HashTable in Java?", a: "HashMap | HashTable | HashMap is not synchronized thereby making it better for non-threaded applications. | HashTable is synchronized and hence it is suitable for threaded applications. | Allows only one null key but any number of null in the values. | This does not allow null in both keys or values. | Supports order of insertion by making use of its subclass LinkedHashMap. | Order of insertion is not guaranteed in HashTable." },
            { q: "What is the importance of reflection in Java?", a: "- The termreflection is used for describing the inspection capability of a code on other code either of itself or of its system and modify it during runtime. - Consider an example where we have an object of unknown type and we have a method ‘fooBar()’ which we need to call on the object. The static typing system of Java doesn't allow this method invocation unless the type of the object is known beforehand. This can be achieved using reflection which allows the code to scan the object and identify if it has any method called “fooBar()” and only then call the method if needed. Method methodOfFoo = fooObject.getClass().getMethod(\"fooBar\", null); methodOfFoo.invoke(fooObject, null); - Using reflection has its own cons: - - Speed — Method…" },
            { q: "What are the different ways of threads usage?", a: "- We can define and implement a thread in java using two ways: - - Extending the Thread class class InterviewBitThreadExample extends Thread{ public void run(){ System.out.println(\"Thread runs...\"); } public static void main(String args[]){ InterviewBitThreadExample ib = new InterviewBitThreadExample(); ib.start(); } } - Implementing the Runnable interface class InterviewBitThreadExample implements Runnable{ public void run(){ System.out.println(\"Thread runs...\"); } public static void main(String args[]){ Thread ib = new Thread(new InterviewBitThreadExample()); ib.start(); } } - Implementing a thread using the method of Runnable interface is more preferred and advantageous as Java does not have support for multiple inheritances of classes.…" },
            { q: "What are the different types of Thread Priorities in Java? And what is the default priority of a thread assigned by JVM?", a: "There are a total of 3 different types of priority available in Java. MIN_PRIORITY: It has an integer value assigned with 1.MAX_PRIORITY: It has an integer value assigned with 10.NORM_PRIORITY: It has an integer value assigned with 5. In Java, Thread with MAX_PRIORITY gets the first chance to execute. But the default priority for any thread is NORM_PRIORITY assigned by JVM." },
            { q: "What is the difference between the program and the process?", a: "- A program can be defined as a line of code written in order to accomplish a particular task. Whereas the process can be defined as the programs which are under execution. - A program doesn't execute directly by the CPU. First, the resources are allocated to the program and when it is ready for execution then it is a process." },
            { q: "What is the difference between the ‘throw’ and ‘throws’ keyword in java?", a: "- The ‘throw’ keyword is used to manually throw the exception to the calling method. - And the ‘throws’ keyword is used in the function definition to inform the calling method that this method throws the exception. So if you are calling, then you have to handle the exception. Example - class Main { public static int testExceptionDivide(int a, int b) throws ArithmeticException{ if(a == 0 | b == 0) throw new ArithmeticException(); return a/b; } public static void main(String args[]) { try{ testExceptionDivide(10, 0); } catch(ArithmeticException e){ //Handle the exception } } } Here in the above snippet, the method testExceptionDivide throws an exception. So if the main method is calling it then it must have handled the exception. Otherwise…" },
            { q: "What are the differences between constructor and method of a class in Java?", a: "Constructor | Method | Constructor is used for initializing the object state. | Method is used for exposing the object's behavior. | Constructor has no return type. | Method should have a return type. Even if it does not return anything, return type is void. | Constructor gets invoked implicitly. | Method has to be invoked on the object explicitly. | If the constructor is not defined, then a default constructor is provided by the java compiler. | If a method is not defined, then the compiler does not provide it. | The constructor name should be equal to the class name. | The name of the method can have any name or have a class name too. | A constructor cannot be marked as final because whenever a class is inherited, the constructors are…" },
            { q: "Identify the output of the below java program and Justify your answer.", a: "class Main { public static void main(String args[]) { Scaler s = new Scaler(5); } } class InterviewBit{ InterviewBit(){ System.out.println(\" Welcome to InterviewBit \"); } } class Scaler extends InterviewBit{ Scaler(){ System.out.println(\" Welcome to Scaler Academy \"); } Scaler(int x){ this(); super(); System.out.println(\" Welcome to Scaler Academy 2\"); } } The above code will throw the compilation error. It is because the super() is used to call the parent class constructor. But there is the condition that super() must be the first statement in the block. Now in this case, if we replace this() with super() then also it will throw the compilation error. Because this() also has to be the first statement in the block. So in conclusion, we can…" },
            { q: "Java works as “pass by value” or “pass by reference” phenomenon?", a: "Java always works as a “pass by value”. There is nothing called a “pass by reference” in Java. However, when the object is passed in any method, the address of the value is passed due to the nature of object handling in Java. When an object is passed, a copy of the reference is created by Java and that is passed to the method. The objects point to the same memory location. 2 cases might happen inside the method: - Case 1: When the object is pointed to another location: In this case, the changes made to that object do not get reflected the original object before it was passed to the method as the reference points to another location. For example: class InterviewBitTest{ int num; InterviewBitTest(int x){ num = x; } InterviewBitTest(){ num =…" },
            { q: "What is the ‘IS-A ‘ relationship in OOPs java?", a: "‘IS-A’ relationship is another name for inheritance. When we inherit the base class from the derived class, then it forms a relationship between the classes. So that relationship is termed an ‘IS-A’ Relationship. Example - Consider a Television (Typical CRT TV). Now another Smart TV that is inherited from television class. So we can say that the Smart iv is also a TV. Because CRT TV things can also be done in the Smart TV. So here ‘IS-A’ Relationship formed. [ SmartTV ‘IS-A’ TV ]." },
            { q: "Which among String or String Buffer should be preferred when there are lot of updates required to be done in the data?", a: "StringBuffer is mutable and dynamic in nature whereas String is immutable. Every updation / modification of String creates a new String thereby overloading the string pool with unnecessary objects. Hence, in the cases of a lot of updates, it is always preferred to use StringBuffer as it will reduce the overhead of the creation of multiple String objects in the string pool." },
            { q: "How to not allow serialization of attributes of a class in Java?", a: "- In order to achieve this, the attribute can be declared along with the usage oftransient keyword as shown below: public class InterviewBitExample { private transient String someInfo; private String name; private int id; // : // Getters setters // : } - In the above example, all the fields exceptsomeInfo can be serialized." },
            { q: "What happens if the static modifier is not included in the main method signature in Java?", a: "There wouldn't be any compilation error. But then the program is run, since the JVM cant map the main method signature, the code throws “NoSuchMethodError” error at the runtime." },
            { q: "Consider the below program, identify the output, and also state the reason for that.", a: "public class Main{ public static void main(String[] args) { System.out.println(\" Hello. Main Method. \"); } public static void main(int[] args) { System.out.println(\" Hello. Main Method2. \"); } } The output of the above program will be Hello. Main Method. This is because JVM will always call the main method based on the definition it already has. Doesn't matter how many main methods we overload it will only execute one main method based on its declaration in JVM." },
            { q: "Can we make the main() thread a daemon thread?", a: "In java multithreading, the main() threads are always non-daemon threads. And there is no way we can change the nature of the non-daemon thread to the daemon thread." },
            { q: "What happens if there are multiple main methods inside one class in Java?", a: "The program can't compile as the compiler says that the method has been already defined inside the class." },
            { q: "What do you understand by Object Cloning and how do you achieve it in Java?", a: "- It is the process of creating an exact copy of any object. In order to support this, a java class has to implement the Cloneable interface of java.lang package and override the clone() method provided by the Object class the syntax of which is: protected Object clone() throws CloneNotSupportedException{ return (Object)super.clone(); } - In case the Cloneable interface is not implemented and just the method is overridden, it results in CloneNotSupportedException in Java." },
            { q: "How does an exception propagate in the code?", a: "When an exception occurs, first it searches to locate the matching catch block. In case, the matching catch block is located, then that block would be executed. Else, the exception propagates through the method call stack and goes into the caller method where the process of matching the catch block is performed. This propagation happens until the matching catch block is found. If the match is not found, then the program gets terminated in the main method." },
            { q: "How do exceptions affect the program if it doesn't handle them?", a: "Exceptions are runtime errors. Suppose we are making an android application with java. And it all works fine but there is an exceptional case when the application tries to get the file from storage and the file doesn’t exist (This is the case of exception in java). And if this case is not handled properly then the application will crash. This will be a bad experience for users. This is the type of error that cannot be controlled by the programmer. But programmers can take some steps to avoid this so that the application won’t crash. The proper action can be taken at this step." },
            { q: "Is it mandatory for a catch block to be followed after a try block?", a: "No, it is not necessary for a catch block to be present after a try block. - A try block should be followed either by a catch block or by a finally block. If the exceptions likelihood is more, then they should be declared using the throws clause of the method." },
            { q: "Will the finally block get executed when the return statement is written at the end of try block and catch block as shown below?", a: "public int someMethod(int i){ try{ //some statement return 1; }catch(Exception e){ //some statement return 999; }finally{ //finally block statements } } finally block will be executed irrespective of the exception or not. The only case where finally block is not executed is when it encounters ‘System.exit()’ method anywhere in try/catch block." },
            { q: "Can you call a constructor of a class inside the another constructor?", a: "Yes, the concept can be termed as constructor chaining and can be achieved usingthis()." },
            { q: "Contiguous memory locations are usually used for storing actual values in an array but not in ArrayList. Explain.", a: "In the case of ArrayList, data storing in the form of primitive data types (like int, float, etc.) is not possible. The data members/objects present in the ArrayList have references to the objects which are located at various sites in the memory. Thus, storing of actual objects or non-primitive data types (like Integer, Double, etc.) takes place in various memory locations. However, the same does not apply to the arrays. Object or primitive type values can be stored in arrays in contiguous memory locations, hence every element does not require any reference to the next element." },
            { q: "Why does the java array index start with 0?", a: "It is because the 0 index array avoids the extra arithmetic operation to calculate the memory address. Example - Consider the array and assume each element takes 4-byte memory space. Then the address will be like this - Now if we want to access index 4. Then internally java calculates the address using the formula- [Base Address + (index * no_of_bytes)]. So according to this. The starting address of the index 4 will be - [100 + (4*4)] = 116. And exactly that's what the address is calculated. Now consider the same with 1 index Array - Now if we apply the same formula here. Then we get - 116 as the starting address of the 4th index. Which is wrong. Then we need to apply formula - [Base Address + ((index-1) * no_of_bytes)]. And for…" },
            { q: "Why is the remove method faster in the linked list than in an array?", a: "In the linked list, we only need to adjust the references when we want to delete the element from either end or the front of the linked list. But in the array, indexes are used. So to manage proper indexing, we need to adjust the values from the array So this adjustment of value is costlier than the adjustment of references. Example - To Delete from the front of the linked list, internally the references adjustments happened like this. The only thing that will change is that the head pointer will point to the head’s next node. And delete the previous node. That is the constant time operation. Whereas in the ArrayList, internally it should work like this- For deletion of the first element, all the next element has to move to one place…" },
            { q: "How does the size of ArrayList grow dynamically? And also state how it is implemented internally.", a: "ArrayList is implemented in such a way that it can grow dynamically. We don't need to specify the size of ArrayList. For adding the values in it, the methodology it uses is - 1. Consider initially that there are 2 elements in the ArrayList. [2, 3]. 2. If we need to add the element into this. Then internally what will happen is- - ArrayList will allocate the new ArrayList of Size (current size + half of the current size). And add the old elements into the new. Old - [2, 3], New - [2, 3, null]. - Then the new value will be inserted into it. [2, 3, 4, null]. And for the next time, the extra space will be available for the value to be inserted. 3. This process continues and the time taken to perform all of these is considered as the amortized…" },
            { q: "How many overloaded add() and addAll() methods are available in the List interface? Describe the need and uses.", a: "There are a total of 4 overloaded methods for add() and addAll() methods available in List Interface. The below table states the description of all. | Return Type | Method Description | boolean | add(Element e): This method is used for adding the element at the end of the List. The Datatype of the element is of any type it has been initially assigned with. It returns the boolean indicating successfully inserted or not. | void | add(int index, Element e): This method is the overloaded version of add() method. In this, along with the element, the index is also passed to the method for the specific index the value needs to be inserted. | boolean | addAll(Collection c): This method helps to add all elements at the end of collections from the…" }
          ],
        },
      ],
    },
    {
      id: "ib-experienced",
      title: "InterviewBit — Experienced",
      icon: "trophy",
      blocks: [
        { type: 'p', text: 'Questions curated from InterviewBit (interviewbit.com), with concise, interview-ready answers.' },
        {
          type: 'qa',
          items: [
            { q: "Although inheritance is a popular OOPs concept, it is less advantageous than composition. Explain.", a: "Inheritance lags behind composition in the following scenarios: - Multiple-inheritance is not possible in Java. Classes can only extend from one superclass. In cases where multiple functionalities are required, for example - to read and write information into the file, the pattern of composition is preferred. The writer, as well as reader functionalities, can be made use of by considering them as the private members. - Composition assists in attaining high flexibility and prevents breaking of encapsulation. - Unit testing is possible with composition and not inheritance. When a developer wants to test a class composing a different class, then Mock Object can be created for signifying the composed class to facilitate testing. This technique…" },
            { q: "What is the difference between ‘>>’ and ‘>>>’ operators in java?", a: "These 2 are the bitwise right shift operators. Although both operators look similar. But there is a minimal difference between these two right shift operators. - ‘>>’ Bitwise Right Shift Operator- This operator shifts each bit to its right position. And this maintains the signed bit. - ‘>>>’ Bitwise Right Shift Operator with trailing zero- This operator also shifts each bit to its right. But this doesn’t maintain the signed bit. This operator makes the Most significant bit to 0. Example- Num1 = 8, Num2 = -8. So the binary form of these numbers are - Num1 = 00000000 00000000 00000000 00001000 Num2 = 11111111 11111111 11111111 11111000 ‘>>’ Operator : 8 >> 1 (Shift by one bit) : Num1 = 00000000 00000000 00000000 00000100Num2 = 11111111…" },
            { q: "What are Composition and Aggregation? State the difference.", a: "Composition, and Aggregation help to build (Has - A - Relationship) between classes and objects. But both are not the same in the end. Let’s understand with the help of an example. - Consider the University as a class that has some departments in it. So the university will be the container object. And departments in it will contain objects. Now in this case, if the container object destroys then the contained objects will also get destroyed automatically. So here we can say that there is a strong association between the objects. So this Strong Association is called Composition. - Now consider one more example. Suppose we have a class department and there are several professors' objects there in the department. Now if the department class…" },
            { q: "How is the creation of a String using new() different from that of a literal?", a: "When a String is formed as a literal with the assistance of an assignment operator, it makes its way into the String constant pool so that String Interning can take place. This same object in the heap will be referenced by a different String if the content is the same for both of them. public bool checking() { String first = \"InterviewBit\"; String second = \"InterviewBit\"; if (first == second) return true; else return false; } The checking() function will return true as the same content is referenced by both the variables. Conversely, when a String formation takes place with the help of a new() operator, interning does not take place. The object gets created in the heap memory even if the same content object is present. public bool…" },
            { q: "How is the ‘new’ operator different from the ‘newInstance()’ operator in java?", a: "Both ‘new’ and ‘newInstance()’ operators are used to creating objects. The difference is- that when we already know the class name for which we have to create the object then we use a new operator. But suppose we don’t know the class name for which we need to create the object, Or we get the class name from the command line argument, or the database, or the file. Then in that case we use the ‘newInstance()’ operator. The ‘newInstance()’ keyword throws an exception that we need to handle. It is because there are chances that the class definition doesn’t exist, and we get the class name from runtime. So it will throw an exception." },
            { q: "Is exceeding the memory limit possible in a program despite having a garbage collector?", a: "Yes, it is possible for the program to go out of memory in spite of the presence of a garbage collector. Garbage collection assists in recognizing and eliminating those objects which are not required in the program anymore, in order to free up the resources used by them. In a program, if an object is unreachable, then the execution of garbage collection takes place with respect to that object. If the amount of memory required for creating a new object is not sufficient, then memory is released for those objects which are no longer in the scope with the help of a garbage collector. The memory limit is exceeded for the program when the memory released is not enough for creating new objects. Moreover, exhaustion of the heap memory takes place…" },
            { q: "Why is synchronization necessary? Explain with the help of a relevant example.", a: "Concurrent execution of different processes is made possible by synchronization. When a particular resource is shared between many threads, situations may arise in which multiple threads require the same shared resource. Synchronization assists in resolving the issue and the resource is shared by a single thread at a time. Let’s take an example to understand it more clearly. For example, you have a URL and you have to find out the number of requests made to it. Two simultaneous requests can make the count erratic. No synchronization: package anonymous; public class Counting { private int increase_counter; public int increase() { increase_counter = increase_counter + 1; return increase_counter; } } If a thread Thread1 views the count as 10…" },
            { q: "In the given code below, what is the significance of ... ?", a: "public void fooBarMethod(String... variables){ // method code } - Ability to provide... is a feature called varargs (variable arguments) which was introduced as part of Java 5. - The function having... in the above example indicates that it can receive multiple arguments of the datatype String. - For example, the fooBarMethod can be called in multiple ways and we can still have one method to process the data as shown below: fooBarMethod(\"foo\", \"bar\"); fooBarMethod(\"foo\", \"bar\", \"boo\"); fooBarMethod(new String[]{\"foo\", \"var\", \"boo\"}); public void myMethod(String... variables){ for(String variable : variables){ // business logic } }" },
            { q: "What will be the output of the below java program and define the steps of Execution of the java program with the help of the below code?", a: "class InterviewBit{ int i; static int j; { System.out.println(\" Instance Block 1. Value of i = \"+i); } static{ System.out.println(\" Static Block 1. Value of j = \"+j); method_2(); } { i = 5; } static{ j = 10; } InterviewBit(){ System.out.println(\" Welcome to InterviewBit \"); } public static void main(String[] args){ InterviewBit ib = new InterviewBit(); } public void method_1(){ System.out.println(\" Instance method. \"); } static{ System.out.println(\" Static Block 2. Value of j = \"+j); } { System.out.println(\" Instance Block 2. Value of i = \"+i); method_1(); } public static void method_2(){ System.out.println(\" Static method. \"); } } The Output we get by executing this program will be Static Block 1. Value of j = 0Static method. Static Block…" },
            { q: "Define System.out.println().", a: "System.out.println() is used to print the message on the console. System - It is a class present in java.lang package. Out is the static variable of type PrintStream class present in the System class. println() is the method present in the PrintStream class. So if we justify the statement, then we can say that if we want to print anything on the console then we need to call the println() method that was present in PrintStream class. And we can call this using the output object that is present in the System class." },
            { q: "Can you explain the Java thread lifecycle?", a: "Java thread life cycle is as follows: - New – When the instance of the thread is created and the start() method has not been invoked, the thread is considered to be alive and hence in the NEW state. - Runnable – Once the start() method is invoked, before the run() method is called by JVM, the thread is said to be in RUNNABLE (ready to run) state. This state can also be entered from the Waiting or Sleeping state of the thread. - Running – When the run() method has been invoked and the thread starts its execution, the thread is said to be in a RUNNING state. - Non-Runnable (Blocked/Waiting) – When the thread is not able to run despite the fact of its aliveness, the thread is said to be in a NON-RUNNABLE state. Ideally, after some time of its…" },
            { q: "What could be the tradeoff between the usage of an unordered array versus the usage of an ordered array?", a: "- The main advantage of having an ordered array is the reduced search time complexity ofO(log n) whereas the time complexity in an unordered array isO(n). - The main drawback of the ordered array is its increased insertion time which is O(n) due to the fact that its element has to reordered to maintain the order of array during every insertion whereas the time complexity in the unordered array is only O(1). - Considering the above 2 key points and depending on what kind of scenario a developer requires, the appropriate data structure can be used for implementation." },
            { q: "Is it possible to import the same class or package twice in Java and what happens to it during runtime?", a: "It is possible to import a class or package more than once, however, it is redundant because the JVM internally loads the package or class only once." },
            { q: "In case a package has sub packages, will it suffice to import only the main package? e.g. Does importing of com.myMainPackage.* also import com.myMainPackage.mySubPackage.*?", a: "This is a big NO. We need to understand that the importing of the sub-packages of a package needs to be done explicitly. Importing the parent package only results in the import of the classes within it and not the contents of its child/sub-packages." },
            { q: "Will the finally block be executed if the code System.exit(0) is written at the end of try block?", a: "NO. The control of the program postSystem.exit(0) is immediately gone and the program gets terminated which is why the finally block never gets executed." },
            { q: "What do you understand by marker interfaces in Java?", a: "Marker interfaces, also known as tagging interfaces are those interfaces that have no methods and constants defined in them. They are there for helping the compiler and JVM to get run time-related information regarding the objects." },
            { q: "Explain the term “Double Brace Initialisation” in Java?", a: "This is a convenient means of initializing any collections in Java. Consider the below example. import java.util.HashSet; import java.util.Set; public class IBDoubleBraceDemo{ public static void main(String[] args){ Set<String> stringSets = new HashSet<String>() { { add(\"set1\"); add(\"set2\"); add(\"set3\"); } }; doSomething(stringSets); } private static void doSomething(Set<String> stringSets){ System.out.println(stringSets); } } In the above example, we see that the stringSets were initialized by using double braces. - The first brace does the task of creating an anonymous inner class that has the capability of accessing the parent class’s behavior. In our example, we are creating the subclass of HashSet so that it can use the add() method…" },
            { q: "Why is it said that the length() method of String class doesn't return accurate results?", a: "- The length method returns the number of Unicode units of the String. Let's understand what Unicode units are and what is the confusion below. - We know that Java uses UTF-16 for String representation. With this Unicode, we need to understand the below two Unicode related terms: - - Code Point: This represents an integer denoting a character in the code space. - Code Unit: This is a bit sequence used for encoding the code points. In order to do this, one or more units might be required for representing a code point. - Under the UTF-16 scheme, the code points were divided logically into 17 planes and the first plane was called the Basic Multilingual Plane (BMP). The BMP has classic characters - U+0000 to U+FFFF. The rest of the characters-…" },
            { q: "What is the output of the below code and why?", a: "public class InterviewBit{ public static void main(String[] args) { System.out.println('b' + 'i' + 't'); } } “bit” would have been the result printed if the letters were used in double-quotes (or the string literals). But the question has the character literals (single quotes) being used which is why concatenation wouldn't occur. The corresponding ASCII values of each character would be added and the result of that sum would be printed.The ASCII values of ‘b’, ‘i’, ‘t’ are: - ‘b’ = 98 - ‘i’ = 105 - ‘t’ = 116 98 + 105 + 116 = 319 Hence 319 would be printed." },
            { q: "What are the possible ways of making object eligible for garbage collection (GC) in Java?", a: "First Approach: Set the object references to null once the object creation purpose is served. public class IBGarbageCollect { public static void main (String [] args){ String s1 = \"Some String\"; // s1 referencing String object - not yet eligible for GC s1 = null; // now s1 is eligible for GC } } Second Approach: Point the reference variable to another object. Doing this, the object which the reference variable was referencing before becomes eligible for GC. public class IBGarbageCollect { public static void main(String [] args){ String s1 = \"To Garbage Collect\"; String s2 = \"Another Object\"; System.out.println(s1); // s1 is not yet eligible for GC s1 = s2; // Point s1 to other object pointed by s2 /* Here, the string object having the…" },
            { q: "Assume a thread has a lock on it, calling the sleep() method on that thread will release the lock?", a: "A thread that has a lock won't be released even after it calls sleep(). Despite the thread sleeping for a specified period of time, the lock will not be released." },
            { q: "What is a Memory Leak? Discuss some common causes of it.", a: "The Java Garbage Collector (GC) typically removes unused objects when they are no longer required, but when they are still referenced, the unused objects cannot be removed. So this causes the memory leak problem. Example - Consider a linked list like the structure below - In the above image, there are unused objects that are not referenced. But then also Garbage collection will not free it. Because it is referencing some existing referenced object. So this can be the situation of memory leak. Some common causes of Memory leaks are - - When there are Unbounded caches. - Excessive page swapping is done by the operating system. - Improper written custom data structures. - Inserting into a collection object without first deleting it.etc." },
            { q: "What are the different categories of Java Design patterns?", a: "Java Design patterns are categorized into the following different types. And those are also further categorized as Structural patterns: - Adapter - Bridge - Filter - Composite - Decorator - Facade - Flyweight - Proxy Behavioral patterns: - Interpreter - Template method/ pattern - Chain of responsibility - Command pattern - Iterator pattern - Strategy pattern - Visitor pattern J2EE patterns: - MVC Pattern - Data Access Object pattern - Front controller pattern - Intercepting filter pattern - Transfer object pattern Creational patterns: - Factory method/Template - Abstract Factory - Builder - Prototype - Singleton" },
            { q: "How we can set the spring bean scope. And what supported scopes does it have?", a: "A scope can be set by an annotation such as the @Scope annotation or the \"scope\" attribute in an XML configuration file. Spring Bean supports the following five scopes: - Singleton - Prototype - Request - Session - Global-session" },
            { q: "What is the best way to inject dependency? Also, state the reason.", a: "There is no boundation for using a particular dependency injection. But the recommended approach is - Setters are mostly recommended for optional dependencies injection, and constructor arguments are recommended for mandatory ones. This is because constructor injection enables the injection of values into immutable fields and enables reading them more easily." },
            { q: "In the below Java Program, how many objects are eligible for garbage collection?", a: "class Main{ public static void main(String[] args){ int[][] num = new int[3][]; num[0] = new int[5]; num[1] = new int[2]; num[2] = new int[3]; num[2] = new int[5]; num[0] = new int[4]; num[1] = new int[3]; num = new int[2][]; } } In the above program, a total of 7 objects will be eligible for garbage collection. Let’s visually understand what's happening in the code. In the above figure on line 3, we can see that on each array index we are declaring a new array so the reference will be of that new array on all the 3 indexes. So the old array will be pointed to by none. So these three are eligible for garbage collection. And on line 4, we are creating a new array object on the older reference. So that will point to a new array and older…" }
          ],
        },
      ],
    }
];
